export type picture = {
  author: {
    id: number;
    name: string;
  };

  authrId: number;
  created: string;
  id: number;
  imageUrl: string;
  locationId: number;
  location: {
    id: number;
    location: string;
  };
  name: string;
};
